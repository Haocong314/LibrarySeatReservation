# 14-管理端与权限开发记录

## 阶段概述

- **阶段编号**：14a
- **阶段名称**：管理端、权限与状态回流开发
- **执行日期**：2026-07-13
- **对应 Sprint**：Sprint 2（管理端核心功能）
- **执行结果**：7/8 张任务卡已完成代码对齐（T02-01 ~ T02-07），1 张待运行时验证（T02-08）

---

## 本轮任务卡清单

| 卡片编号 | 标题 | 状态 | 本轮新增 |
|----------|------|------|----------|
| T02-01 | 实现 IAdminService / AdminService | ✅ 待验证 | 是 |
| T02-02 | 创建管理端 ViewModel 类 | ✅ 待验证 | 是 |
| T02-03 | 实现 P06 管理员登录页 | ✅ 待验证 | 是 |
| T02-04 | 实现 P07 预约管理页 | ✅ 待验证 | 是 |
| T02-05 | 实现 P08 座位管理页 | ✅ 待验证 | 是 |
| T02-06 | 实现 P09 统计页 | ✅ 待验证 | 是 |
| T02-07 | 实现管理端侧边栏导航与布局 | ✅ 待验证 | 是 |
| T02-08 | Sprint 2 端到端走查 | 📋 待开发 | — |

**说明**：本轮是基于 Sprint 2 第 1 轮执行的代码对齐审查，管理端源码由上游预先创建。本轮的增量开发包括：Bug 修复（状态筛选值）、侧边栏布局从零创建、视图 Layout 绑定。

---

## 本轮修改点清单

### 页面 / View

| 文件 | 操作 | 说明 |
|------|------|------|
| `Views/Shared/_AdminLayout.cshtml` | 新建 | 管理端侧边栏布局，含 4 导航项（预约管理/座位管理/统计/退出）、当前页高亮 |
| `Views/AdminManage/Reservations.cshtml` | 修改 | 添加 `Layout = "_AdminLayout"`；修复 status 筛选 option 值 `Active`/`Cancelled` → `已预约`/`已取消`；修正 selected 逻辑 |
| `Views/AdminManage/Seats.cshtml` | 修改 | 添加 `Layout = "_AdminLayout"` |
| `Views/AdminManage/Stats.cshtml` | 修改 | 添加 `Layout = "_AdminLayout"` |

### Controller

| 文件 | 操作 | 说明 |
|------|------|------|
| `Controllers/AdminController.cs` | 审查通过 | Login GET/POST + Logout，Session["AdminId"] 管理 |
| `Controllers/AdminManageController.cs` | 审查通过 | EnsureAdmin() 权限守护应用于全部 Action；Reservations/Seats/Stats/CancelReservation/ToggleSeat |

### Service

| 文件 | 操作 | 说明 |
|------|------|------|
| `Services/AdminService.cs` | 审查通过 | ValidateLogin（DB SHA256 校验）、GetReservationsAsync（date/status/areaId/keyword 多条件筛选）、CancelReservationAsync（管理员无归属限制）、GetAllSeatsAsync（含 Area 关联）、ToggleSeatStatusAsync（可用↔停用）、GetStatsAsync（4 项全量 DB 统计） |
| `Services/IAdminService.cs` | 审查通过 | 接口定义完整 |

### DataAccess / Entity

| 文件 | 操作 | 说明 |
|------|------|------|
| `Models/Entities/Admin.cs` | 审查通过 | Id/Username/PasswordHash 字段对齐 docs/08 |
| `Data/AppDbContext.cs` | 审查通过 | Admins DbSet 已注册 |

### ViewModel

| 类 | 文件 | 说明 |
|------|------|------|
| `AdminLoginViewModel` | `Models/ViewModels/` | 管理员登录表单 |
| `AdminReservationItem` | `Models/ViewModels/` | 预约管理列表行 |
| `AdminSeatItem` | `Models/ViewModels/` | 座位管理列表行（含 Floor/AreaName/设施图标） |
| `AdminStatsViewModel` | `Models/ViewModels/` | 统计页数据容器（总计/可用/今天活跃/今天取消数 + 区域分布列表） |
| `AreaStatItem` | `Models/ViewModels/` | 单区域统计项 |

### 侧边栏布局

| 文件 | 说明 |
|------|------|
| `Views/Shared/_AdminLayout.cshtml` | 220px 左侧深色侧边栏 + flex 布局主机；路由匹配高亮当前导航；响应式样式（.admin-layout/.admin-sidebar/.admin-main） |

---

## 本轮 Bug 修复

| 编号 | 描述 | 影响 | 修复方式 |
|------|------|------|----------|
| BUG-14a-01 | `Reservations.cshtml` 状态筛选 `<option>` 值为英文 `Active`/`Cancelled`，不符合 docs/08 中文状态定义 | 筛选功能失效 | 改为 `已预约`/`已取消`，selected 逻辑改为 Razor 条件块 |
| BUG-14a-02 | `Reservations.cshtml` 中 `<option selected="@(...)">` 字符串语法错误 | 初载状态筛选默认值不正确 | 改为 `@if(ViewBag.CurrentStatus == "...")` 显式输出 `selected="selected"` |

---

## 权限控制实现验证

| 检查项 | 结果 | 说明 |
|--------|------|------|
| EnsureAdmin() 守卫 | ✅ 通过 | AdminManageController 所有 Action 开头调用，未登录重定向 /Admin/Login |
| AdminController.Login | ✅ 通过 | SHA256 哈希校验 + Session["AdminId"] 赋值 |
| 用户端不访问管理端 | ✅ 通过 | 管理端路由 /AdminManage/* 需 AdminId Session，用户端不使用 |
| 管理端取消无归属限制 | ✅ 通过 | CancelReservationAsync 不校验 userId，符合设计 |
| Logout 清 Session | ✅ 通过 | Session.Clear() + RedirectToAction("Login") |

---

## 状态回流验证

| 场景 | 回流路径 | 验证方式 |
|------|----------|----------|
| 管理员取消预约 | DB Status='已取消' → `GetMyReservationsAsync` 查询 | P05 刷新可见 |
| 管理员停用座位 | DB Status='停用' → `GetSeatsByAreaAsync` 过滤 | P02 不显示 |
| 管理员启用座位 | DB Status='可用' → `GetSeatsByAreaAsync` 包含 | P02 显示 |

---

## 与设计文档对齐检查

| 检查项 | docs/05 | docs/08 | docs/09 | 实现状态 |
|--------|---------|---------|---------|----------|
| P06 登录页 | ✅ | — | ✅ §3.2 | ✅ 对齐 |
| P07 预约管理页 | ✅ | — | ✅ §3.2 | ✅ 对齐 |
| P08 座位管理页 | ✅ §P08 | — | — | ✅ 对齐 |
| P09 统计页 | ✅ | ✅ §8 | — | ✅ 对齐 |
| 状态值"已预约"/"已取消"/"可用"/"停用" | ✅ | ✅ §4.3 | — | ✅ 对齐 |
| Admin.Username / PasswordHash | — | ✅ §4.2 | — | ✅ 对齐 |
| 管理端侧边栏布局 | ✅ §管理端要求 | — | — | ✅ 已实现 |

---

## 当前阶段完成度判断

| 阶段最低完成线 | 状态 | 说明 |
|---------------|------|------|
| 管理员可登录 | ⚠️ 待运行时验证 | AdminController.Login 代码审查通过 |
| 能筛选预约、处理状态、管理座位 | ⚠️ 待运行时验证 | AdminManageController 全部 Action 代码审查通过 |
| 用户端页面能看到管理端处理后的结果变化 | ⚠️ 待运行时验证 | 状态回流链路完整（Service 层 DB 真读写），待运行时确认 |

**总体判断**：⚠️ 未达到阶段最低完成线（运行时验证未完成）

**原因**：当前环境缺少 .NET SDK，无法执行 `dotnet build` / `dotnet run` 进行编译验证和端到端走查（T02-08）。

---

## 下一轮优先任务建议

1. **先决条件**：安装 .NET 8 SDK
2. **T02-08**：执行 Sprint 2 端到端走查（P06→P07→P08→P09 + 状态回流联动验证）
3. 通过 T02-08 后进入阶段 **14b 审计**
