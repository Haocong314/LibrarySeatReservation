# 13 — 用户端主链路开发审计报告

## 审计信息

| 属性 | 内容 |
|------|------|
| 审计阶段 | 13b |
| 审计对象 | README.md, docs/13-用户端主链路开发记录.md, docs/项目任务板与迭代记录.md, src/ |
| 实现依据 | docs/05, docs/08, docs/09, docs/12 |
| 审计日期 | 2026-07-13 |
| 审计范围 | 仅审阶段13新增结果，不重做阶段12或更早阶段的全量审计 |

---

## A. 总体判断

**⚠️ 有条件通过** — 代码层面完全对齐设计文档，字段命名、状态值、页面命名、分层架构全部符合 docs/05/08/09 规范。唯一阻塞项：环境无 .NET SDK，无法执行 `dotnet build` 和运行时端到端走查（T01-09）。代码审查级别的审计已全部通过。

---

## B. 发现的问题清单

| 编号 | 严重级 | 问题描述 | 对应审计项 |
|------|--------|----------|-----------|
| IS-01 | ⚠️ 中 | 环境无 .NET SDK，无法执行编译验证和运行时走查 | 审计项 4/5/6 |
| IS-02 | 🟢 低 | ReservationController.Create POST 中 `detail` 变量在 GetSeatDetailAsync 失败时为 null，后续 `detail?.SeatNumber ?? ""` 使用空字符串做降级处理，逻辑安全但降级体验不佳 | 审计项 2 |
| IS-03 | 🟢 低 | `docs/13-用户端主链路开发记录.md` 中 Sprint 1 的 T01-09 未标记状态变更（仍为"待开发"），与任务板其他 8 张卡片已标记"待验证"不一致 | 审计项 8 |

---

## C. 必改项

| 编号 | 问题 | 修改方案 |
|------|------|----------|
| **B-01** | 环境缺少 .NET SDK | 安装 .NET 8 SDK，执行 `dotnet build` 确认编译通过，执行 `dotnet run` 完成 T01-09 端到端走查 |

> 说明：B-01 为环境类必改项，非代码缺陷。代码本身经人工审查未发现语法或逻辑错误。

---

## D. 建议优化项

| 编号 | 问题 | 建议 |
|------|------|------|
| S-01 | ReservationController.Create 中 ~30 行 ViewModel 构建逻辑重复出现两次（GET 中展示空表单、POST 失败后重新展示） | 抽取为私有方法 `BuildCreateViewModel(seatId, date, ...)` |
| S-02 | `docs/13-用户端主链路开发记录.md` 中可补充 T01-09 任务卡状态说明，保持与任务板一致 | 下一轮更新时统一修正 |

---

## E. 是否可以进入下一步"管理端、权限与状态回流"

**⚠️ 不可直接进入。**

理由：阶段 13 最低完成线（至少一次真实写库 + 真实查库 + 用户在"我的预约"看到结果）目前仅完成代码级验证，尚未完成运行时验证（T01-09）。按照 13a 提示词规则："如果本轮结束后仍未达到阶段最低完成线，不进入下一阶段"。

**建议路径**：
1. 安装 .NET 8 SDK → `dotnet build` 验证编译 → `dotnet ef database update` 建库 → `dotnet run` 启动
2. 执行 T01-09 端到端走查：P01(选用户2024001) → P02(A区) → P03(A-01选空闲时段) → P04(提交预约) → P05(查看记录+取消)
3. 走查通过后，将 T01-09 标记为"已完成"，此时阶段 13 最低完成线达标
4. 然后进入阶段 14a（管理端开发）

---

## 审计详细对照表

### 字段名对齐检查

| 设计文档字段 | 源码字段 | 一致性 |
|-------------|---------|--------|
| docs/08: `ReservationDate` (DATE) | `Reservation.ReservationDate` (DateTime) | ✅ |
| docs/08: `Status` = '已预约'/'已取消' | `Reservation.Status` = "已预约"/"已取消" | ✅ |
| docs/08: `Status` = '可用'/'停用' | `Seat.Status` = "可用"/"停用" | ✅ |
| docs/08: `Floor` (INT) | `SeatArea.Floor` (int) | ✅ |
| docs/05: P02 "座位列表页" | `Seat/Index` | ✅ |
| docs/05: P03 "座位详情页" | `Seat/Detail` | ✅ |
| docs/05: P04 "预约提交页" | `Reservation/Create` | ✅ |
| docs/05: P05 "我的预约页" | `Reservation/MyReservations` | ✅ |

### 真实数据库读写检查

| 操作 | 方法 | 是否写库 | 是否读库 | 用户归属 |
|------|------|---------|---------|---------|
| 提交预约 | `ReservationService.CreateReservationAsync` | ✅ `_db.Reservations.Add` + `SaveChangesAsync` | ✅ 冲突检查 `AnyAsync` | ✅ `userId` 参数 |
| 查看我的预约 | `ReservationService.GetMyReservationsAsync` | — | ✅ `Where(r => r.UserId == userId)` | ✅ 按 userId 过滤 |
| 取消预约 | `ReservationService.CancelReservationAsync` | ✅ `SaveChangesAsync` | ✅ `FindAsync` | ✅ 含 `res.UserId != userId` 检查 |
| 浏览座位 | `SeatService.GetSeatsByAreaAsync` | — | ✅ `Where(s => s.AreaId == areaId)` | — |
| 查看详情 | `SeatService.GetSeatDetailAsync` | — | ✅ 关联查询 Seat+Area+Reservations | — |

### 用户闭环连通性检查

| 链路 | 状态 |
|------|------|
| P01 → P02 (Home → Seat/Index) | ✅ `/Seat/Index` 可接收 areaId 参数 |
| P02 → P03 (Index → Detail) | ✅ 座位卡片链接指向 `/Seat/Detail/{id}` |
| P03 → P04 (Detail → Create) | ✅ "去预约"按钮指向 `/Reservation/Create?seatId={id}` |
| P04 → P05 (Create → MyReservations) | ✅ POST 成功后 `RedirectToAction("MyReservations")` |
| P05 取消 → P05 刷新 | ✅ POST 取消后 `RedirectToAction("MyReservations")` |

---

## 历史问题记录（非阻塞）

| 编号 | 问题 | 来源 | 建议处理阶段 |
|------|------|------|------------|
| PB-01~PB-05 | 开发前阻塞项（PRD 与 DB 对齐、P08/P09 工程落点等） | 阶段 11 审计 | 各 P1 项已在进行中或延后至 Sprint 2/4 处理 |

---

*审计人：AI 开发助理 | 审计时间：2026-07-13*
