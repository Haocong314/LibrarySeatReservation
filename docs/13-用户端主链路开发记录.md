# 13 — 用户端主链路开发记录

## 阶段信息

| 属性 | 内容 |
|------|------|
| 阶段编号 | 13a |
| 阶段名称 | 用户端主链路开发 |
| Sprint | Sprint 1 |
| 日期 | 2026-07-13 |
| 对应提示词 | 13a-用户端主链路开发提示词.txt |

---

## 一、本轮任务卡清单

本轮从 Sprint 1 的 9 张任务卡中选取 3 张最小可闭环任务卡推进：**T01-01（SeatService）、T01-02（ReservationService）、T01-03（ViewModels）**。选择理由：这三张卡片是用户端主链路（P01→P05）的数据层基础，必须先对齐才能推动后续页面。

### 已完成任务卡

| 编号 | 标题 | 当前状态 | 本轮验证 |
|------|------|----------|----------|
| T01-01 | 实现 ISeatService / SeatService | ✅ 待验证 | 代码已对齐设计文档 |
| T01-02 | 实现 IReservationService / ReservationService | ✅ 待验证 | 代码已对齐设计文档 |
| T01-03 | 创建 ViewModel 类 | ✅ 待验证 | 字段类型已对齐设计文档 |
| T01-04 | P01 用户首页 | ✅ 待验证 | 已有骨架，无需修改 |
| T01-05 | P02 座位列表页 | ✅ 待验证 | 视图已对齐（中文化状态、Floor→int） |
| T01-06 | P03 座位详情页 | ✅ 待验证 | 视图已对齐 |
| T01-07 | P04 预约提交页 | ✅ 待验证 | Controller bug 已修复 |
| T01-08 | P05 我的预约页 | ✅ 待验证 | 视图已对齐（Date→ReservationDate、状态中文化） |

### 未完成

| 编号 | 标题 | 原因 |
|------|------|------|
| T01-09 | Sprint 1 端到端走查 | 需要 dotnet run 运行时验证；当前环境无 .NET SDK |

---

## 二、本轮修改点清单

### 2.1 Entity 层

| 文件 | 修改内容 |
|------|----------|
| `Models/Entity/Reservation.cs` | `Date` → `ReservationDate`；Status 默认值 `"Active"` → `"已预约"` |
| `Models/Entity/Seat.cs` | Status 默认值 `"Available"` → `"可用"` |
| `Models/Entity/SeatArea.cs` | Floor 类型 `string` → `int` |

### 2.2 Data 层

| 文件 | 修改内容 |
|------|----------|
| `Data/AppDbContext.cs` | 移除 SeatArea.Floor 的 `HasMaxLength`（已改为 int）；唯一索引从 `Date`→`ReservationDate`；状态值中文化 |
| `Data/DbInitializer.cs` | 区域名改为 `"A区 自习室(3F)"` / `"B区 阅览区(4F)"`；Floor 改为 int 值 3/4；状态中文化 |

### 2.3 ViewModel 层

| 文件 | 修改内容 |
|------|----------|
| `Models/ViewModel/ViewModels.cs` | `SeatListItem.Floor`: `string` → `int`；`SeatDetail.Floor`: `string` → `int`；`MyReservationItem.Date` → `ReservationDate`；`AdminReservationItem.Date` → `ReservationDate`；`AdminSeatItem.Floor`: `string` → `int` |

### 2.4 Service 层

| 文件 | 修改内容 |
|------|----------|
| `Services/SeatService.cs` | 状态值全部中文化（`"已预约"` / `"可用"` / `"停用"`）；Floor 改为 int；日期字段改为 `ReservationDate` |
| `Services/ReservationService.cs` | 同上 + 修复冲突检查逻辑 |
| `Services/AdminService.cs` | 同上 + `ToggleSeatStatusAsync` 切换值改为 `"可用"↔"停用"` |

### 2.5 Controller 层

| 文件 | 修改内容 |
|------|----------|
| `Controllers/ReservationController.cs` | 修复 `Create` POST 方法中引用未定义变量 `detail` 的 bug |

### 2.6 View 层

| 文件 | 修改内容 |
|------|----------|
| `Views/Seat/Index.cshtml` | 状态判断 `"Available"`→`"可用"`；Floor 显示加 `F` 后缀 |
| `Views/Seat/Detail.cshtml` | 状态判断 `"Available"`→`"可用"`；Floor 显示加 `F` 后缀 |
| `Views/Reservation/MyReservations.cshtml` | `res.Date`→`res.ReservationDate`；状态判断 `"Active"`→`"已预约"` |
| `Views/AdminManage/Reservations.cshtml` | `res.Date`→`res.ReservationDate`；状态筛选值中文化 |
| `Views/AdminManage/Seats.cshtml` | 状态判断 `"Available"`→`"可用"`；Floor 显示加 `F` 后缀 |

---

## 三、本轮验证结果

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 字段名对齐 docs/08 | ✅ | ReservationDate、Status 中文、Floor int 均已对齐 |
| 状态值中文化 | ✅ | 全部统一为：已预约 / 已取消 / 可用 / 停用 |
| Controller bug 修复 | ✅ | ReservationController.Create POST 不再引用未定义变量 |
| 编译检查 | ⚠️ | 环境无 .NET SDK，无法执行 `dotnet build`；代码逻辑审查通过 |
| 运行时验证 | ⚠️ | 无法 `dotnet run`，需在有 SDK 环境验证 |

---

## 四、当前阶段完成度判断

### 阶段最低完成线检查

| 条件 | 状态 |
|------|------|
| 至少一次真实写库 | ⚠️ ReservationService.CreateReservationAsync 已实现，含 `_db.Reservations.Add` + `SaveChangesAsync`，需运行时验证 |
| 至少一次真实查库 | ⚠️ SeatService.GetSeatsByAreaAsync / ReservationService.GetMyReservationsAsync 已实现，需运行时验证 |
| 用户能完成一次预约并在"我的预约"中看到结果 | ⚠️ P01→P02→P03→P04→P05 链路代码已就绪，需运行时走查验证 |

**结论：代码层面已满足最低完成线的全部条件，但因环境无 .NET SDK 无法执行运行时验证。建议在有 SDK 环境完成 T01-09 端到端走查后，确认进入阶段 13b 审计。**

---

## 五、下一轮优先任务建议

1. **T01-09**：Sprint 1 端到端走查（需 dotnet run 环境）
2. **13b**：用户端主链路开发审计
3. 若走查发现阻塞性 Bug，优先修复后再进入审计

---

## 六、默认补足项 / 当前假设

本轮未使用默认补足项。所有字段名、状态值、交互逻辑均以 docs/05、docs/08、docs/09 为准。唯一需要澄清的是：

- **假设**：用户端"体验账号下拉切换"机制沿用骨架实现（HomeController 中 Session 切换），本轮未修改此逻辑。
