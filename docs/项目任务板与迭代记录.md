# 图书馆座位预约系统 — 项目任务板与迭代记录

## 任务卡编号规则

`T{XX}-{YY}` — XX = Sprint 编号（00-04），YY = 该 Sprint 内任务卡序号（01 起）

---

## 看板总览

| 状态 | 说明 |
|------|------|
| 📋 待开发 | 已规划但尚未开始 |
| 🔧 进行中 | 当前正在开发 |
| ✅ 待验证 | 开发完成，等待评审或自测 |
| 🏁 已完成 | 评审/自测通过，已归档 |

---

# Sprint 0 — 工程骨架搭建

| 属性 | 内容 |
|------|------|
| Sprint 目标 | 完成 `.sln`、`.csproj`、NuGet 包引用、AppDbContext、EF Core Migrations 首次建库建表、种子数据初始化 |
| 阶段最低完成线 | `dotnet build` 通过 + `dotnet run` 可启动 + `dotnet ef database update` 可建库建表 + 种子数据可通过 SSMS 或 `dotnet ef dbcontext list` 验证 |
| 当前轮次 | 第 1 轮（初始） |
| 任务统计 | 总计 6 张卡片 |
| 当前完成率 | 0% |

---

## Sprint 0 任务卡

### T00-01：创建解决方案文件 (.sln) 和 Web 项目 (.csproj)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-01 |
| 卡片标题 | 创建解决方案文件 (.sln) 和 Web 项目 (.csproj) |
| 验收标准 | `src/` 目录下存在 `LibrarySeatReservation.sln`；`src/LibrarySeatReservation.Web/` 下存在 `.csproj` 文件；`dotnet restore` 成功 |
| 关联页面 | 无（工程级别） |
| 关联文档 | docs/07-系统设计说明.md §3（项目目录结构建议） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T00-02：添加 NuGet 包引用

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-02 |
| 卡片标题 | 添加 NuGet 包引用（EF Core + SQL Server） |
| 验收标准 | `.csproj` 中包含 `Microsoft.EntityFrameworkCore.SqlServer`、`Microsoft.EntityFrameworkCore.Tools`、`Microsoft.EntityFrameworkCore.Design`；`dotnet list package` 可见全部包 |
| 关联页面 | 无 |
| 关联文档 | docs/08-数据库设计.md（SQL Server LocalDB） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T00-03：配置 Program.cs 并首次 dotnet build

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-03 |
| 卡片标题 | 配置 Program.cs（MVC + Session + DbContext）并首次 dotnet build |
| 验收标准 | `dotnet build` 成功无错误（0 Error）；`Program.cs` 中已注册 `AddControllersWithViews()`、`AddSession()`、`AddDbContext<AppDbContext>()` |
| 关联页面 | 无 |
| 关联文档 | docs/07-系统设计说明.md §4（分层说明）、docs/09-关键链路详细设计.md §2.3 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T00-04：创建 Entity 类与 AppDbContext

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-04 |
| 卡片标题 | 创建 5 个 Entity 类 + AppDbContext（含 Fluent API 唯一索引配置） |
| 验收标准 | `Models/Entities/` 下存在 `User.cs`、`Admin.cs`、`SeatArea.cs`、`Seat.cs`、`Reservation.cs`；`Data/AppDbContext.cs` 中 `OnModelCreating` 配置了外键和过滤唯一索引 `IX_Reservations_Conflict` |
| 关联页面 | 全 9 页 |
| 关联文档 | docs/08-数据库设计.md §4（表结构设计） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T00-05：首次 EF Core Migration 创建数据库

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-05 |
| 卡片标题 | 执行首次 EF Core Migration 并创建数据库 |
| 验收标准 | `dotnet ef migrations add InitialCreate` 成功生成 `Migrations/` 目录及迁移文件；`dotnet ef database update` 成功创建 SQL Server LocalDB 数据库；通过 SSMS 或 `dotnet ef dbcontext list` 可确认 5 张表存在 |
| 关联页面 | 无 |
| 关联文档 | docs/08-数据库设计.md §4、docs/10-开发准备与Sprint0.md |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T00-06：实现种子数据初始化

| 字段 | 内容 |
|------|------|
| 卡片编号 | T00-06 |
| 卡片标题 | 实现 DbInitializer.Seed() 种子数据初始化 |
| 验收标准 | 应用启动后数据库中自动插入：3 个体验用户（2024001/2024002/2024003）、1 个管理员（admin/admin123 的 SHA256 哈希）、2 个区域（A区自习室 / B区阅览室）、7 个座位（A-01 ~ A-05 + B-01 ~ B-02）；通过 SSMS SELECT 可验证 |
| 关联页面 | P01（体验用户下拉）、P06（管理员登录） |
| 关联文档 | docs/08-数据库设计.md §4.5（种子数据）、README.md §演示账号 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

---

# Sprint 1 — 用户端核心功能（主 Sprint，可多轮推进）

| 属性 | 内容 |
|------|------|
| Sprint 目标 | 完成用户端 5 页面（P01-P05）Razor View + Controller + Service，实现用户预约主链路闭环 |
| 阶段最低完成线 | P01→P02→P03→P04→P05 页面全部可运行；能在 P04 成功提交预约并跳转至 P05 查看 |
| 当前轮次 | 第 1 轮 |
| 任务统计 | 总计 9 张卡片 |
| 本轮新增完成卡片 | T01-01, T01-02, T01-03 |
| 当前完成率 | 88.9%（8/9 待验证，1 未完成） |
| 是否允许进入下一阶段 | ⚠️ 待运行时验证通过后允许 |

## Sprint 1 任务卡

### T01-01：创建 ISeatService / SeatService

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-01 |
| 卡片标题 | 实现 ISeatService / SeatService（含 GetSeatsByAreaAsync、GetSeatForReservationAsync） |
| 验收标准 | `GetSeatsByAreaAsync(areaId)` 返回指定区域的座位列表（含占用状态标记）；`GetSeatForReservationAsync(seatId, date)` 返回座位详情 + 今日已占时段列表 |
| 关联页面 | P02、P03 |
| 关联文档 | docs/09-关键链路详细设计.md §2.3 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-02：创建 IReservationService / ReservationService

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-02 |
| 卡片标题 | 实现 IReservationService / ReservationService（含 CreateReservationAsync、GetMyReservationsAsync、CancelByUserAsync） |
| 验收标准 | `CreateReservationAsync` 含冲突检查 + 事务 + 插入；`GetMyReservationsAsync(userId)` 返回该用户的有效预约列表；`CancelByUserAsync(userId, reservationId)` 含所属人检查 |
| 关联页面 | P04、P05 |
| 关联文档 | docs/09-关键链路详细设计.md §2.3、§4 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-03：创建 ViewModel 类

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-03 |
| 卡片标题 | 创建用户端 ViewModel 类（SeatListVM、SeatDetailVM、ReservationCreateVM、MyReservationsVM） |
| 验收标准 | `Models/ViewModels/` 下存在 4 个类；`SeatDetailVM` 包含 `OccupiedTimeSlots` 字段；`ReservationCreateVM` 包含 `TimeSlotOptions` 选项列表 |
| 关联页面 | P02-P05 |
| 关联文档 | docs/09-关键链路详细设计.md §2.2 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-04：实现 P01 用户首页 (Home/Index)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-04 |
| 卡片标题 | 实现 P01 用户首页 Razor View（HomeController.Index） |
| 验收标准 | `/` 或 `/Home/Index` 显示"找座位"入口 + 体验账号下拉框（2024001/2024002/2024003） + 今日开放时间；页面布局遵循 prototype/static-v1/index.html 的卡片 + 按钮风格 |
| 关联页面 | P01 用户首页 |
| 关联文档 | docs/05-页面卡与UI规范.md §P01 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-05：实现 P02 座位列表页 (Seat/List)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-05 |
| 卡片标题 | 实现 P02 座位列表页 Razor View（SeatController.List） |
| 验收标准 | `/Seat/List` 或 `/Seat/List?areaId=1` 展示该区域的座位卡片（座位号、是否有电源/灯光图标）；每张卡片可点击跳转 P03；空区域显示"该区域暂无座位" |
| 关联页面 | P02 座位列表页 |
| 关联文档 | docs/05-页面卡与UI规范.md §P02 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-06：实现 P03 座位详情页 (Seat/Detail)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-06 |
| 卡片标题 | 实现 P03 座位详情页 Razor View（SeatController.Detail） |
| 验收标准 | `/Seat/Detail/3` 展示座位信息（编号、区域、设施图标） + 今日时段占用矩阵（7 个时段、灰色=已占/绿色=空闲）；"去预约"按钮跳转 P04 |
| 关联页面 | P03 座位详情页 |
| 关联文档 | docs/05-页面卡与UI规范.md §P03 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-07：实现 P04 预约提交页 (Reservation/Create)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-07 |
| 卡片标题 | 实现 P04 预约提交页 Razor View（ReservationController.Create GET + POST） |
| 验收标准 | GET 显示日期选择器 + 时段下拉（只显示空闲时段）+ 座位摘要；POST 成功后跳转 P05，失败时留在本页显示错误（"该时段已被预约"）；ModelState 校验空输入 |
| 关联页面 | P04 预约提交页 |
| 关联文档 | docs/09-关键链路详细设计.md §2.2 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-08：实现 P05 我的预约页 (Reservation/MyReservations)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-08 |
| 卡片标题 | 实现 P05 我的预约页 Razor View（ReservationController.MyReservations + Cancel POST） |
| 验收标准 | GET 展示当前用户的预约列表（座位号、日期、时段、状态标签）；"取消"按钮 POST 后本页刷新；空状态显示"暂无预约记录" |
| 关联页面 | P05 我的预约页 |
| 关联文档 | docs/05-页面卡与UI规范.md §P05、docs/09-关键链路详细设计.md §4 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T01-09：Sprint 1 端到端走查

| 字段 | 内容 |
|------|------|
| 卡片编号 | T01-09 |
| 卡片标题 | Sprint 1 端到端走查：P01 → P02 → P03 → P04 → P05 全链路 |
| 验收标准 | 在 Chrome 手机模式下：选用户 2024001 → 找座位 → 选 A区 → 点 A-01 → 看时段 08:00-10:00 空闲 → 预约 → 成功跳转 P05 → 列表可见预约记录 → 取消 → 列表变为空 |
| 关联页面 | P01-P05 |
| 关联文档 | docs/04-页面树与业务流程.md §4（用户主链路） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

---

## Sprint 1 站会记录

### Sprint 1 第 1 轮站会 — 日期：2026-07-13

**昨日完成：**
- （首轮）完成 Sprint 0 工程骨架搭建（阶段 12a）

**今日计划：**
1. T01-01 实现 ISeatService / SeatService — 字段对齐设计文档
2. T01-02 实现 IReservationService / ReservationService — 字段对齐设计文档
3. T01-03 创建 ViewModel 类 — 字段类型对齐设计文档

**阻塞项：**
- 环境无 .NET SDK，无法执行 `dotnet build` 和 `dotnet run`，运行时机验证无法完成

## Sprint 1 评审记录

### Sprint 1 评审记录 — 日期：2026-07-13

**参与人**：开发者 + AI 开发助理

**Sprint 目标达成情况：**
- 用户端 5 页 Razor View + Controller + Service：⚠️ 部分达成（代码已就绪，待运行时验证）
- 预约主链路闭环：⚠️ 部分达成（代码链路完整，待运行时验证）

**本轮新增完成卡片：**
- T01-01 实现 ISeatService / SeatService
- T01-02 实现 IReservationService / ReservationService
- T01-03 创建 ViewModel 类

**演示内容：**
- （无法运行时演示）代码审查通过：Entity/Service/Controller/View 全部字段名、状态值已对齐 docs/05、docs/08、docs/09

**评审结论：** ⚠️ 有条件通过（条件：需在有 .NET SDK 环境完成 T01-09 端到端走查）

## Sprint 1 回顾记录

### Sprint 1 回顾记录 — 日期：2026-07-13

**做得好的：**
- 严格遵循设计文档（docs/05/08/09）对齐字段名和状态值
- Entity → Service → Controller → View 全链路同步修改，保持一致性
- 发现并修复 ReservationController.Create 中的未定义变量 bug

**可改进的：**
- 环境准备：开发环境缺少 .NET SDK，导致无法编译验证和运行时测试
- 建议下一轮前安装 .NET 8 SDK

**下个 Sprint 的行动项：**
- 安装 .NET 8 SDK 后执行 T01-09 端到端走查
- 通过后进入阶段 13b 审计

---

# Sprint 2 — 管理端核心功能（主 Sprint，可多轮推进）

| 属性 | 内容 |
|------|------|
| Sprint 目标 | 完成管理端 4 页面（P06-P09）Razor View + Controller + Service，实现管理员取消预约链路 |
| 阶段最低完成线 | P06 可登录 → P07 可筛选并取消预约 → P08 可启用/停用座位 → P09 显示统计数据 |
| 当前轮次 | 第 1 轮（初始） |
| 任务统计 | 总计 8 张卡片 |
| 本轮新增完成卡片 | 无 |
| 当前完成率 | 0% |
| 是否允许进入下一阶段 | —（首轮未开始） |

## Sprint 2 任务卡

### T02-01：实现 IAdminService / AdminService

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-01 |
| 卡片标题 | 实现 IAdminService / AdminService（含 ValidateAsync、GetReservationsAsync、CancelReservationAsync） |
| 验收标准 | `ValidateAsync(username, password)` 校验 SHA256 哈希返回 Admin 或 null；`GetReservationsAsync(date, status, areaId, userId)` 支持多条件筛选；`CancelReservationAsync(reservationId)` 不做所属人检查 |
| 关联页面 | P06、P07 |
| 关联文档 | docs/09-关键链路详细设计.md §3.3 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-02：创建管理端 ViewModel 类

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-02 |
| 卡片标题 | 创建管理端 ViewModel 类（AdminLoginVM、ReservationsVM、SeatsVM、StatsVM） |
| 验收标准 | `Models/ViewModels/` 下新增管理端 4 个 ViewModel 类；`StatsVM` 包含 `TotalSeats`、`TodayReservations`、`TodayCancellations`、`AreaDistribution` |
| 关联页面 | P06-P09 |
| 关联文档 | docs/08-数据库设计.md §8（统计字段与统计口径） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-03：实现 P06 管理员登录页 (Admin/Login)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-03 |
| 卡片标题 | 实现 P06 管理员登录页 Razor View（AdminController.Login GET + POST） |
| 验收标准 | 输入 admin/admin123 → 登录成功跳转 P07 → Session 存入 AdminId；密码错误显示"用户名或密码错误"；未登录直接访问 P07/P08/P09 重定向到 P06 |
| 关联页面 | P06 管理员登录页 |
| 关联文档 | docs/09-关键链路详细设计.md §3.2 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-04：实现 P07 预约管理页 (AdminManage/Reservations)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-04 |
| 卡片标题 | 实现 P07 预约管理页 Razor View（AdminManageController.Reservations GET + CancelReservation POST） |
| 验收标准 | 页面加载显示全部预约表格（日期、时段、用户、座位、状态标签）；可按日期/状态/区域筛选；点击"取消"确认后刷新列表；空表格显示"暂无预约记录" |
| 关联页面 | P07 预约管理页 |
| 关联文档 | docs/09-关键链路详细设计.md §3.2 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-05：实现 P08 座位管理页 (AdminManage/Seats)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-05 |
| 卡片标题 | 实现 P08 座位管理页 Razor View（AdminManageController.Seats GET + ToggleSeat POST） |
| 验收标准 | 页面加载显示全部座位表格（编号、区域、状态）；"启用"/"停用"按钮可切换座位状态；停用的座位在 P02 不显示 |
| 关联页面 | P08 座位管理页 |
| 关联文档 | docs/05-页面卡与UI规范.md §P08 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-06：实现 P09 统计页 (AdminManage/Stats)

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-06 |
| 卡片标题 | 实现 P09 统计页 Razor View（AdminManageController.Stats GET） |
| 验收标准 | 页面显示：总座位数卡片、今日预约数卡片、今日取消数卡片、各区域座位分布表/卡片；数据与数据库一致 |
| 关联页面 | P09 统计页 |
| 关联文档 | docs/08-数据库设计.md §8（统计口径） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-07：实现管理端侧边栏导航与布局

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-07 |
| 卡片标题 | 实现管理端侧边栏布局（_AdminLayout.cshtml） |
| 验收标准 | P07/P08/P09 共享侧边栏（预约管理 / 座位管理 / 统计 / 退出登录）；当前页面菜单项高亮；桌面端宽度≥1024px 显示侧边栏 |
| 关联页面 | P07、P08、P09 |
| 关联文档 | docs/05-页面卡与UI规范.md §管理端布局要求 |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

### T02-08：Sprint 2 端到端走查

| 字段 | 内容 |
|------|------|
| 卡片编号 | T02-08 |
| 卡片标题 | Sprint 2 端到端走查：P06 → P07 → P08 → P09 全链路 |
| 验收标准 | 登录 → P07 可筛选到 Sprint 1 创建的预约 → 取消预约 → P05 刷新可见状态变为"已取消"；P08 停用 A-01 → P02 不再显示 A-01；P09 数字与操作一致 |
| 关联页面 | P06-P09 + P02 P05 联动 |
| 关联文档 | docs/04-页面树与业务流程.md §5（管理主链路） |
| 当前状态 | 📋 待开发 |
| 本轮是否验证通过 | ⚠️ 代码已对齐，待运行时验证 |

---

# Sprint 3 — 完善与 Bug 修复（主 Sprint，可多轮推进）

| 属性 | 内容 |
|------|------|
| Sprint 目标 | 全链路端到端测试、边角场景修复、UI 一致性微调、响应式适配确认 |
| 阶段最低完成线 | 用户链路和管理链路各完成 3 轮完整走查无阻塞性 Bug |
| 当前轮次 | 第 1 轮（初始） |
| 任务统计 | 总计 5 张卡片 |
| 本轮新增完成卡片 | 无 |
| 当前完成率 | 0% |
| 是否允许进入下一阶段 | —（首轮未开始） |

## Sprint 3 任务卡

| 编号 | 标题 | 状态 |
|------|------|------|
| T03-01 | 全链路回归测试：用户预约链路 3 轮走查 | 📋 待开发 |
| T03-02 | 全链路回归测试：管理链路 3 轮走查 | 📋 待开发 |
| T03-03 | 异常边界场景补测（并发冲突、座位停用后预约、Session 过期操作等） | 📋 待开发 |
| T03-04 | UI 一致性微调（统一圆角、间距、颜色变量引用、按钮风格） | 📋 待开发 |
| T03-05 | 手机端/桌面端响应式适配确认与修复 | 📋 待开发 |

---

# Sprint 4 — 最终交付（主 Sprint，可多轮推进）

| 属性 | 内容 |
|------|------|
| Sprint 目标 | 演示准备、README 完善、已知限制文档化、代码清理、最终打标 |
| 阶段最低完成线 | 可在全新机器上按 README 步骤跑通全流程 |
| 当前轮次 | 第 1 轮（初始） |
| 任务统计 | 总计 4 张卡片 |
| 本轮新增完成卡片 | 无 |
| 当前完成率 | 0% |
| 是否允许进入下一阶段 | —（终末阶段） |

## Sprint 4 任务卡

| 编号 | 标题 | 状态 |
|------|------|------|
| T04-01 | README 完整更新（已实现范围、演示账号、运行步骤、截图占位、已知限制） | 📋 待开发 |
| T04-02 | 代码清理（删除注释代码、统一 using 排序、移除未使用 import） | 📋 待开发 |
| T04-03 | 演示走查演练（至少在全新 clone 的环境演练 1 次） | 📋 待开发 |
| T04-04 | 合并 dev → main 并打 Git Tag `v1.0.0` | 📋 待开发 |

---

# 模板

## 个人站会记录模板

```
### Sprint X 第 Y 轮站会 — 日期：YYYY-MM-DD

**昨日完成：**
1. [卡片编号] 卡片标题
2. [卡片编号] 卡片标题

**今日计划：**
1. [卡片编号] 卡片标题

**阻塞项：**
- 无 / [描述阻塞]
```

## Sprint 评审记录模板

```
### Sprint X 评审记录 — 日期：YYYY-MM-DD

**参与人**：[姓名]

**Sprint 目标达成情况：**
- [目标1]：✅ 达成 / ⚠️ 部分达成 / ❌ 未达成

**本轮新增完成卡片：**
- [卡片编号] 卡片标题

**演示内容：**
- [操作路径描述]

**评审结论：** ✅ 通过 / ⚠️ 有条件通过（条件：...）/ ❌ 不通过
```

## Sprint 回顾记录模板

```
### Sprint X 回顾记录 — 日期：YYYY-MM-DD

**做得好的：**
- 

**可改进的：**
- 

**下个 Sprint 的行动项：**
- 
```

---

# 阶段 11 — 开发前一致性总审计结果

> 审计文档：docs/11-开发前一致性总审计.md
> 审计日期：2026-07-10

## 开发前阻塞项清单

| 编号 | 阻塞级别 | 描述 | 回改文档 | 清零状态 |
|------|----------|------|----------|----------|
| PB-01 | P1 | PRD §11 TimeSlot 列为独立实体，DB 设计不建独立表 | docs/03-PRD-Lite.md | ⬜ 未处理 |
| PB-02 | P1 | PRD §11 User 含 Role 字段合并，DB 设计 Admin 独立建表 | docs/03-PRD-Lite.md | ⬜ 未处理 |
| PB-03 | P1 | PRD §11 Reservation 含 TimeSlotId FK，DB 设计改为 VARCHAR | docs/03-PRD-Lite.md | ⬜ 未处理 |
| PB-04 | P1 | docs/09 未覆盖 P08 座位管理和 P09 统计页工程落点 | docs/09-关键链路详细设计.md | ⬜ 未处理 |
| PB-05 | P1 | 成功标准 S1-S7 与验收标准 AC-01~AC-08 无显式交叉引用 | docs/01 或 docs/03 | ⬜ 未处理 |

## 一致性修订项清单

| 编号 | 修订内容 | 涉及文档 | 建议完成节点 |
|------|----------|----------|------------|
| CR-01 | TimeSlot 从"独立实体"改为"预定义固定值（VARCHAR 存储于 Reservations）" | docs/03 §11 | Sprint 0 开始前 |
| CR-02 | User/Admin 拆分说明：下游采用 Users + Admins 两表 | docs/03 §11 | Sprint 0 开始前 |
| CR-03 | Reservation 字段：TimeSlotId → TimeSlot VARCHAR；新增 CancelledAt | docs/03 §11 | Sprint 0 开始前 |
| CR-04 | 补充 P08 座位管理 / P09 统计页简短工程落点说明 | docs/09 末尾 | Sprint 2 开始前 |
| CR-05 | 建立成功标准 ↔ 验收标准映射表 | docs/01 或 docs/03 | Sprint 4 收尾 |

## 清零状态说明

| 状态 | 含义 |
|------|------|
| ⬜ 未处理 | 已识别但尚未执行修订 |
| 🔧 已处理 | 修订已完成，等待复核 |
| ✅ 已复核 | 修订经审计确认通过 |

## 开发前总审计结论

**✅ 有条件 Go — 允许进入 Sprint 0（工程骨架搭建）**

- **无 P0 阻塞项**：所有执行级文档（07/08/09）+ 任务板自洽
- **3 个 P1 需 Sprint 0 前回改**：docs/03 PRD 实体模型与 DB 设计对齐（PB-01/02/03）
- **2 个 P1 可延后**：PB-04 在 Sprint 2 前补、PB-05 在 Sprint 4 收尾补
